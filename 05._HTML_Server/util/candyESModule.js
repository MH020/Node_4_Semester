// we cant import json like we could with common js, we would need to read the file as a string and then parse it 

//ES Module - vi kan have det samme i frontend som vi har i backend, så det bliver nemmere at import og export filer på tværs. util filer 
//EcmaScript ES6 - beslutter nye moduler i js og definerer standarden 

// export const cantImportJSONFiles = true 

export default 1234 // can be a number - doesnt have to be objects 